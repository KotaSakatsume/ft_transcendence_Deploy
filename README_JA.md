*このプロジェクトは、42のカリキュラムの一部として okaname, mkuida, tishihar, kosakats によって作成されました。*

# 5x5 ミニ将棋オンライン

<img width="1907" height="931" alt="image" src="https://github.com/user-attachments/assets/304077c1-b08c-4eba-88c0-8b9b4a6c5e05" />

## 📝 概要

**5x5 ミニ将棋オンライン** は、伝統的な将棋を現代的かつスピーディな 3D 環境で楽しめる Web ベースのプロジェクトです。マルチプレイヤー対戦、AI 練習、そしてプレイヤー間のソーシャルエコシステムをサポートする堅牢なシングルページアプリケーション (SPA) の構築を目標としています。

### 主な機能
- **没入型 3D グラフィックス**: Three.js と Blender モデルを使用したインタラクティブな盤面。
- **リアルタイムエンジン**: WebSocket による即時の指し手同期。
- **セキュアな認証**: OAuth とソルト付きハッシュによるユーザー管理。
- **観戦体験**: コミュニティ向けのライブ対局観戦機能。
- **AI 対戦**: コンピュータ相手の練習モード。

---

## 🚀 起動手順

### 前提条件
- **Docker & Docker Compose**: コンテナ環境の実行に必要です。
- **GNU Make**: 自動化コマンド (Makefile) を使用するために必要です。
- **メモリ**: Docker に最低 2GB の割り当てを推奨します。

### セットアップと起動

1. **リポジトリのクローン**:
   ```bash
   git clone <repository-url>
   cd transcendence
   ```

2. **環境変数の設定**:
   ルートディレクトリに `.env` ファイルを作成します。
   ```bash
   cp .env.example .env
   ```
   *`DATABASE_URL`, `AUTH_SECRET`, `POSTGRES_PASSWORD` などの設定が必要です。*

3. **アプリケーションの起動 (Makefile)**:
   本プロジェクトには、Docker 操作を簡略化するための `Makefile` が含まれています。

   - **本番モード** (42 評価用標準設定):
     ```bash
     make prod
     ```
   - **開発モード** (ホットリロード有効):
     ```bash
     make test
     ```

4. **サービスの停止**:
   ```bash
   make down-prod  # 本番環境の停止
   make down-test  # 開発環境の停止
   ```

5. **ブラウザでアクセス**:
   `http://localhost:8080` でアプリケーションにアクセスできます（Nginx 経由）。

---

## 🛠 技術スタック

### 使用技術とフレームワーク
- **フロントエンド**: **Next.js 14 (App Router)** - SEO サポート、ルーティング効率、React との親和性のために採用。
- **バックエンド**: **Node.js (Next.js API Routes)** - 統一された開発体験と高速な起動を実現。
- **3D エンジン**: **React Three Fiber (Three.js)** & **Blender** - 高性能な盤面・駒の描画および 3D モデル作成に使用。
- **リアルタイム通信**: **Socket.IO** - 対局操作やチャットの双方向通信を実現。
- **ORM**: **Prisma** - DB とアプリケーションロジック間の型安全性を確保。

### 技術選定の理由
ネットワークレイヤー全体で厳密な型定義を行い、実行時エラーを最小限に抑えるために **T3 スタイルスタック** (Next.js + Prisma + TypeScript) を選択しました。また、対局履歴や統計のデータ整合性を維持するため、NoSQL ではなく PostgreSQL を採用しています。

---

## 📊 データベーススキーマ

**PostgreSQL** 上で Prisma を用いてリレーショナルな整合性を維持しています：

- **User**: `id (UUID)`, `email`, `passwordHash`, `totalMatches`, `wins`
- **Match**: `id (UUID)`, `blackUserId (FK)`, `whiteUserId (FK)`, `winnerUserId (FK)`
- **Friendship**: `requesterId (FK)`, `addresseeId (FK)`, `status` (PENDING, ACCEPTED)
- **Session/Account**: OAuth 永続化と JWT ベースのセッション管理用

---

## 📋 機能リストと担当者

- **将棋コアロジック**: `<okaname>` - 指し手のバリデーション、成り、駒取りの実装。
- **3D ボード描画**: `<kosakats>` - 3D モデル作成とアニメーションフック。
- **リアルタイム同期**: `<okaname>` / `<tishihar>` - ルーム管理と指し手ブロードキャストを行う WebSocket サーバー。
- **ユーザー管理 & 認証**: `<mkuida>` - サインアップ/ログインフロー、プロフィール編集、フレンド機能。
- **観戦者 UI**: `<okaname>` / `<kosakats>` - 非参加者向けのグローバル状態管理。
- **AI 対戦相手**: `<okaname>` - オフライン用の指し手検索アルゴリズム。
- **HTTPS**: `<tishihar>` - セキュアな HTTPS 通信の実装。
- **OAuth**: `<mkuida>` - OAuth 認証の実装。
- **利用規約 & プライバシーポリシー**: `<tishihar>` - 利用規約とプライバシーポリシーの実装。
- **チャット**: `<kosakats>` / `<tishihar>` - チャット機能の実装。
- **リモートプレイヤー**: `<okaname>` - リモートプレイヤー機能の実装。

---

## 🧩 モジュールとポイント計算（合計 21 ポイント）

| ポイント | セクション | 種類 | モジュール | 担当者 |
|---|---|---|---|---|
| 🟢 | 1.Web | Major | **フロントエンドとバックエンドの両方にフレームワークを使用** | 全員 |
| 🟢 | 1.Web | Major | **WebSocket によるリアルタイム機能の実装** | `<okaname>` / `<mkuida>` |
| 🟢 | 1.Web | Major | **ユーザー間のインタラクション機能** | `<mkuida>` / `<tishihar>` |
| 🟡 | 1.Web | Minor | **ORM の使用** | `<mkuida>` |
| 🟡 | 1.Web | Minor | **サーバーサイドレンダリング (SSR)** | 全員 |
| 🟡 | 2.アクセシビリティと国際化 | Minor | **追加ブラウザのサポート** | 全員 |
| 🟢 | 3.ユーザー管理 | Major | **標準的なユーザー管理と認証** | `<mkuida>` |
| 🟡 | 3.ユーザー管理 | Major | **OAuth 2.0 によるリモート認証** | `<mkuida>` |
| 🟢 | 4.人工知能 | Major | **AI 対戦相手の導入** | `<okaname>` |
| 🟢 | 6.ゲームとユーザー体験 | Major | **完全な Web ベースゲームの実装（対人戦）** | 全員 |
| 🟢 | 6.ゲームとユーザー体験 | Major | **リモートプレイヤー** | `<okaname>` |
| 🟢 | 6.ゲームとユーザー体験 | Major | **高度な 3D グラフィックスの実装** | `<kosakats>` |
| 🟡 | 6.ゲームとユーザー体験 | Minor | **観戦モードの実装** | `<okaname>` / `<kosakats>` |

> 🟢 = 2 ポイント &nbsp;&nbsp; 🟡 = 1 ポイント

### モジュール説明

#### 1.Web
- **フレームワーク使用 (Major)**: フルスタックフレームワークとして Next.js を採用。App Router による堅牢なフロントエンドレンダリングと、組み込みの API ルートによるバックエンド処理を実現しています。
- **リアルタイム機能 (Major)**: Socket.IO を統合し、対局状態のリアルタイム更新、チャット、プレイヤーの在線状態管理を双方向通信で実現しています。
- **ユーザー間インタラクション (Major)**: プロフィール管理、フレンドリクエスト、リアルタイムチャットを含む包括的なソーシャルエコシステムを実装しています。
- **ORM の使用 (Minor)**: Prisma ORM により PostgreSQL をアプリケーション状態に効率的にマッピング。強力な型付けと SQL インジェクション防止を実現しています。
- **SSR (Minor)**: Next.js の機能を活用し、主要ページをサーバーサイドでレンダリングすることでパフォーマンスと SEO を向上させています。

#### 2.アクセシビリティと国際化
- **追加ブラウザのサポート (Minor)**: UI コンポーネントと WebSocket 機能を徹底的にテストし、Chromium ブラウザだけでなく Mozilla Firefox との完全な互換性を確保しています。

#### 3.ユーザー管理
- **標準的なユーザー管理と認証 (Major)**: ソルト付きパスワードハッシュを利用した安全なサインアップ/ログインフローを実装し、ユーザーが自分のプロフィールを安全に管理できるようにしています。
- **OAuth 2.0 によるリモート認証 (Major)**: OAuth を使用したシームレスなログイン体験を提供し、サードパーティプロバイダーを通じた安全な認証を実現しています。

#### 4.人工知能
- **AI 対戦相手 (Major)**: シングルプレイヤーモード向けに指し手検索ロジックアルゴリズムを開発し、オフラインで自動対戦相手と練習できるようにしています。

#### 6.ゲームとユーザー体験
- **完全な Web ベースゲーム (Major)**: 指し手のバリデーション、成り（「なる」）、駒取りシステムを実装した完全な 5x5 ミニ将棋エンジンを構築しています。
- **リモートプレイヤー (Major)**: WebSocket 統合により、異なるネットワーク間でのプレイヤー間で低遅延かつリアルタイムの盤面状態同期を実現しています。
- **高度な 3D グラフィックス (Major)**: React Three Fiber (Three.js) と Blender で設計したモデルを活用し、カスタム駒とアニメーションを備えた没入型 3D 将棋盤を構築しています。
- **観戦モード (Minor)**: 非参加者がアクティブな対局ルームに参加してリアルタイムで観戦でき、同一の盤面更新とリアルタイムチャットを利用できます。

---

## 🔍 レビュアー確認チェックリスト（追加モジュール・合計 4 ポイント）

評価時に以下のモジュールを重点的に確認してください：

- **ゲームカスタマイズオプション (Minor)**: プレイヤーはゲーム体験をカスタマイズできます。設定が正しく動作することを確認してください。
- **リアルタイム協調機能 (Minor)**: 単純なゲームプレイを超えた高度なライブ同期機能。複数ユーザーの同時操作に基づいてインターフェースが動的に更新されます。
- **サーバーサイドレンダリング (SSR) (Minor)**: Next.js を利用して主要ページをサーバー上でプリレンダリングし、ロードパフォーマンスと SEO を大幅に向上させています。ページソースでサーバーレンダリングされた HTML を確認してください。
- **ゲーム統計と対局履歴 (Minor)**: 完全な対局データをデータベースに保存し、過去の 1v1 対局結果、勝敗記録、現在の順位を確認できます。

---

## 👥 チーム情報

- **Product Owner (PO)**: `<mkuida>` - ロードマップ策定、ユーザーニーズ分析、ソーシャル/認証モジュールの監督。
- **Project Manager (PM)**: `<tishihar>` - タスク管理、マイルストーン管理、Docker 調整、コードレビューの徹底。
- **Technical Lead**: `<kosakats>` - アーキテクチャ指針の策定、Next.js 基盤の構築、3D レンダリングパイプラインと駒の物理挙動の設計。
- **Developer**: `<okaname>` - コアゲームルール、マッチメイキング、WebSocket 接続、ロジックエンジンの実装。

### プロジェクト管理方法

- **作業分担**: モジュール型フィーチャーブランチワークフローを採用。フロントエンド (3D/Next.js UI) とバックエンド (WebSockets/認証) に論理的に分担し、マージコンフリクトを最小化しながら並行開発を進めました。隔週で進捗レビューを実施しました。
- **使用ツール**: **GitHub Issues** とGitHub Projects の統合カンバンボードを使用して、バックログ管理、課題の割り当て、コードレビュー、スプリント目標のトラッキングを行いました。
- **コミュニケーション**: 日次同期、ペアプログラミング、ブロッキング課題の迅速な解決のために **Discord** をメインチャンネルとして活用しました。

---

## 👤 個人の貢献と課題

- **`<okaname>`**: カスタム Prisma アダプタを使用した NextAuth 設定に苦労したが、JWT 用カスタムセッションコールバックの実装で解決。
- **`<mkuida>`**: WebSocket のルーム参加時におけるレースコンディションに直面し、サーバー側のロックとアトミックな状態更新で解決。
- **`<tishihar>`**: Web パフォーマンスのための 3D テクスチャ最適化が課題だったが、圧縮 GLB モデルとインスタンス描画で改善。
- **`<kosakats>`**: 3D と UI の境界を越えた「成り」ロジックの実装に苦労し、論理状態をアニメーションタイマーから分離することで解決。

---

## 🤖 リソースと AI の利用

### AI 利用ポリシー
42 のポリシーに従い、AI ツール（Cursor / Antigravity エージェント）を以下の目的で使用しました：
- **初期化**: アプリの初期セットアップ。
- **リファクタリング**: 将棋エンジン内の冗長なロジックの整理。
- **テスト**: 盤面状態バリデーションのためのエッジケース用モックデータの作成。
- **コンフリクト解消**: ブランチマージ時のビルド成果物の競合解消の自動化。

### 参考文献
- [Next.js ドキュメント](https://nextjs.org/)
- [Prisma リファレンス](https://www.prisma.io/)
- [Socket.IO ドキュメント](https://socket.io/)